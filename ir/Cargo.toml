[package]
name = "ir"
version = "0.1.0"
edition = "2021"
rust-version.workspace = true

[features]
default = []
socket = ["dep:serde"]

[dependencies]
whale-target = { path = "../target-spec" }
serde = { version = "1", features = ["derive"], optional = true }
